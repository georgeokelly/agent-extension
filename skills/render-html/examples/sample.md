# Render HTML Sample

This sample demonstrates a local image asset that should be embedded into the
final standalone HTML document.

![A simple line chart placeholder](assets/figure.svg)

```python
print("render-html")
```
